// Ici, la conversion n'est pas obligatoire à cause de la division.
const distance = Number(prompt("Distance : "));
const temps = Number(prompt("Temps"));

const vitesse = distance / temps;
alert(`${vitesse}m/s`);
